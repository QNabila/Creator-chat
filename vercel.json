{
  "cleanUrls": true,
  "installCommand": "echo \"No install needed for static frontend\"",
  "buildCommand": "rm -rf dist && mkdir -p dist && cp static/index.html dist/index.html && cp -R static dist/static",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
